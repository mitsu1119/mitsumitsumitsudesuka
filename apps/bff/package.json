{
	"name": "bff",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"gen:articles": "tsx --tsconfig tsconfig.scripts.json scripts/generate-article-index.ts",
		"deploy": "pnpm run gen:articles && wrangler deploy",
		"dev": "pnpm run gen:articles && wrangler dev --local --port 8787",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"@types/node": "^25.0.3",
		"tsx": "^4.21.0",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.54.0"
	}
}